@model Vidly.ViewModels.MovieFormViewModel
@{
    ViewBag.Title = "New";
}

<h2>New Movie</h2>
@using (Html.BeginForm("Save", "Movies"))
{
    <div class="form-group">
        @Html.LabelFor(m => m.Movie.Name)
        @Html.TextBoxFor(m => m.Movie.Name, new {@class = "form-control"})
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Movie.ReleaseDate, "Release Date")
        @Html.TextBoxFor(m => m.Movie.ReleaseDate, new {@class = "form-control"})
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Movie.ReleaseDate, "Genre")
        @Html.DropDownListFor(m => m.Movie.GenreId, new SelectList(Model.Genres, "Id", "Name"), "Select Genre", new { @class = "form-control" })
    </div>
    @Html.HiddenFor(m => m.Movie.Id);
    <div class="form-group">
        @Html.LabelFor(m => m.Movie.ReleaseDate, "Number in Stock")
        @Html.TextBoxFor(m => m.Movie.NumberInStock, new { @class = "form-control" })
    </div>
    <button type="submit" class="btn btn-primary">Save</button>
}

